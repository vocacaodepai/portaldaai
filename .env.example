# Chave de API do Pexels (https://www.pexels.com/api/)
# Usada para buscar automaticamente as imagens de capa dos artigos.
# Sem essa chave, o site usa uma capa ilustrada gerada localmente (fallback).
PEXELS_API_KEY=
